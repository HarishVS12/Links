<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="vm"
            type="com.linksofficial.links.viewmodel.FeedVM" />

        <import type="android.view.View" />

    </data>

    <FrameLayout
        android:id="@+id/frame"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">


        <FrameLayout
            android:id="@+id/card_tag"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="6dp"
            android:background="@drawable/ic_background_feed"
            android:clickable="true">

            <!--            android:onClick="@{(v) -> vm.selected(v)}"-->

            <TextView
                android:id="@+id/tv_tags"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:clickable="true"
                android:fontFamily="@font/lato_regular"
                android:paddingHorizontal="4dp"
                android:paddingVertical="10dp"
                android:text="Technology"
                android:textSize="12sp"
                android:textColor="@{vm.isTagFocused? @color/white : @color/black, default=@color/white}"
                android:textStyle="bold"/>
<!--            android:textColor="@color/color_feed_text_selector"-->

        </FrameLayout>

    </FrameLayout>

</layout>
